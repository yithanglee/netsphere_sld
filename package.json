{
    "name": "vite_front",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "start": "vite build && vite preview --host 127.0.0.1 --port 5274 --strictPort"
    },
    "devDependencies": {
        "vite": "^5.4.19"
    },
    "dependencies": {
        "ethers": "^6.15.0"
    }
}
